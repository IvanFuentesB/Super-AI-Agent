# Codex N+3.64 Next Sequence Lock

## Current Lock

Do not merge.

N+3.64 is PENDING TARGET BRANCH because:

`origin/feat/ghoti-agent-claude-n3-63-openfang-moneyprinter-content-runway`

does not exist after fetch and eight polling attempts.

## Exact Next Claude Recommendation

Next Claude task:

`N+3.63A Recovery - Push OpenFang MoneyPrinter Content Runway Branch`

Target branch:

`feat/ghoti-agent-claude-n3-63-openfang-moneyprinter-content-runway`

Required scope:

- Add `03_scripts/external_repo_intake.py`.
- Add `03_scripts/content_money_workflow.py`.
- Add `23_configs/external_repo_intake.example.json`.
- Add `23_configs/content_money_workflow.example.json`.
- Update `03_scripts/ghoti_dashboard.py` truthfully.
- Update `03_scripts/local_worker_router.py` routing.
- Preserve existing LLM Council, bridge, Ruflo, Gemma, Obsidian, language, Rust, and merge-assistant gates.
- Keep OpenFang and MoneyPrinter intake-only.
- Keep content workflow planning-only.

Forbidden:

- no `git clone`
- no package install
- no `pip install`
- no `npm install`
- no Docker launch
- no OpenFang runtime
- no MoneyPrinter runtime
- no external HTTP/API calls
- no `.env` or secret reads
- no browser automation
- no upload/post/publish/send/message/pay/trade/outreach
- no scraping
- no live account actions

## Exact Next Codex Recommendation

After the exact remote branch exists, rerun:

`N+3.64 - OpenFang MoneyPrinter Content Runway Audit`

Codex should audit only:

`origin/feat/ghoti-agent-claude-n3-63-openfang-moneyprinter-content-runway`

Do not substitute another branch unless the operator explicitly changes the target.

## Operator Command Now

Do not merge. First verify/push the target:

```powershell
cd C:\Users\ai_sandbox\Documents\AI_Managed_Only
git fetch origin
git rev-parse origin/feat/ghoti-agent-claude-n3-63-openfang-moneyprinter-content-runway
```

If that fails, Claude/operator must push the N+3.63 branch before Codex can audit it.

## Direct Truth Lock

- OpenFang intake audited? NO, target missing.
- MoneyPrinter intake audited? NO, target missing.
- Content workflow audited? NO, target missing.
- Clone/install/runtime run? NO by Codex.
- Live actions run? NO.
- CC/Codex automatic? NO on known Ghoti design.
- Ruflo runtime wired? NO on known Ghoti design.
- Java tracked? No new evidence; prior truth remains no tracked Java.
- Rust tracked? No new evidence; prior truth remains no tracked Rust.
- Rust rewrite now? NO.
