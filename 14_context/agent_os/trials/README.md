# Agent OS - local worker trial outputs

Generated drafts from the suggestion-only local worker trial harness
(`03_scripts/agent_os/local_worker_trial.py`). Everything here is a
repo-local draft produced under the Rust guard's allow decision; the
harness never executes a command. Generated content is gitignored; only
this README is tracked.
