# Claude Mem / Obsidian-skills / MemPalace — Memory Stack Intake (N+6.30A)

**Milestone:** N+6.30A
**Status:** static planning only — no install, no clone, no execution

---

## Current Ghoti Memory Stack

| Layer | Where | Status |
|-------|-------|--------|
| Memory Vault | `14_context/00_main_memory/` | Active — file-based, committed |
| Compact memory | `14_context/compact_memory/` | Active — context snapshots for LLM handoffs |
| Hermes status packet | `14_context/agent_handoff_vault/` + Hermes memory | Active (N+6.25B merged) |
| Agent relay state | `14_context/agent_relay/` | Active |
| Agent handoff vault | `14_context/agent_handoff_vault/02_Agent_Handoffs/` | Active |
| Context packs | Generated by `ghoti_context_pack_builder.py` | Active |
| Repo knowledge map | Generated by `ghoti_repo_knowledge_map.py` | Active |

The current stack is **file-based, local, no external service, no API key**. Any new
memory tool must remain compatible with this approach or be isolated behind a gate.

---

## Claude Mem

### Status: source_needed

The operator must confirm the exact repo/tool. Several tools use the name "Claude Mem":
- A memory layer for Claude Code session continuity (likely the intended one).
- A personal memory note-taking tool.
- A plugin system for persistent Claude context.

**No URL is guessed. No clone until the operator confirms the source.**

### What we expect it to deliver (if it is the session-continuity tool)
- Persist key facts / context across Claude sessions without relying on context window.
- Complement `14_context/compact_memory/` (which currently does this via committed files).
- Potentially expose a structured API for reading/writing memory entries.

### Evaluation criteria (once source confirmed)
1. Is it file-based and local, or does it require an external service / API key?
2. Can it read/write Ghoti's existing `14_context/` structure without breaking it?
3. Does it require installing npm/pip packages with postinstall hooks?
4. License compatibility with Ghoti's MIT / source-visible approach?

### Safe integration path (if source confirmed and file-based)
- Static inspect: LICENSE, README, install scripts.
- Dry-run in isolated profile against junk memory files.
- If safe: add a Ghoti-native wrapper that reads/writes `14_context/` using the same
  format — no direct dependency on the external tool.
- Gate: `tier1_static_inspect` → isolated trial → Ghoti-native adapter.

### If it requires an external service
- Defer to a secret-management milestone.
- Never commit API keys or auth tokens.
- Local-first alternative: extend `ghoti_context_pack_builder.py`.

---

## Obsidian-skills

### Status: source_needed

The operator must confirm the exact source. "Obsidian-skills" could be:
- A Claude Code skills pack for working with an Obsidian vault.
- A set of Obsidian plugins for agent-friendly knowledge management.
- A bridge between Claude and an Obsidian vault.

**No URL is guessed. No clone until the operator confirms the source.**

### What Ghoti already has for Obsidian
`16_notion/` and the context pack builder already export knowledge in formats
suitable for Obsidian consumption. N+6.22A tool intake noted Obsidian as a Tier-1
shared human/agent memory target.

### Evaluation criteria (once source confirmed)
1. Does it write to the Obsidian vault destructively, or read-only?
2. Can it use the Ghoti `14_context/` structure as its vault source?
3. Does it require Obsidian to be running (plugin) or is it CLI-standalone?
4. Any risk of overwriting operator notes without confirmation?

### Safe integration plan (once source confirmed)
1. Static inspect: license, README, install/plugin mechanics.
2. Read-only path first: configure it to *read* `14_context/` into Obsidian.
3. Write path: only add if writes can be sandboxed to a dedicated `16_obsidian/`
   folder that does not overlap with `14_context/` canonical state.
4. Never enable destructive vault edits without an explicit per-operation approval gate.

### Hard rules for any Obsidian integration
- No destructive writes to the operator's main vault without per-operation approval.
- No access to private Obsidian notes outside the designated Ghoti folder.
- No plugins that call external services without explicit approval and a key gate.
- Vault path never committed; no real local paths in committed files.

---

## MemPalace / Memory-Palace (Agent Memory)

### Disambiguation
There are **two distinct "memory palace" concepts** in the backlog:

| Concept | What it is | Status |
|---------|-----------|--------|
| **PAO app (N+6.24A)** | User-facing memory palace learning app (3D environments, LiDAR, PAO mnemonic system) | Future product; Tier-1-last; heavy privacy surface; NOT this lane |
| **Agent memory palace** | A structured memory store / context graph for AI agents | This lane; source_needed; lightweight |

**Rule:** the PAO app (N+6.24A) is a separate product that should be built last, after
the core supervised agent stack is proven. It carries camera/LiDAR/device capture risks
and belongs to a distinct product lane.

### Status of agent memory palace: source_needed

The operator must confirm whether a specific "memory-palace" or "MemPalace" repo exists
for agent memory (as opposed to the PAO user product). No URL is guessed.

### What a good agent memory palace would provide
- Structured loci (named memory slots with typed content).
- Fast retrieval by key, tag, or semantic proximity.
- Compatible with Ghoti's file-based `14_context/` approach.
- No external DB required.

### If source is confirmed and it is file-based / local
1. Static inspect: license, README, data model.
2. Map its schema onto Ghoti's existing memory structure (`00_main_memory/`,
   `compact_memory/`, `agent_handoff_vault/`).
3. Evaluate whether it replaces, extends, or duplicates the current stack.
4. If additive: create a Ghoti-native wrapper; no direct dependency.

### If no repo is found
Fall back to extending Ghoti's existing memory stack:
- Structured JSON index over `14_context/` files.
- Keyed by milestone, tool, decision, and agent.
- Generated by `ghoti_context_pack_builder.py --memory-index`.
- Queryable by `ghoti_repo_knowledge_map.py`.

---

## Dreams / Memory Consolidation Lane (N+6.30A patch)

### What is a "dream" in this context?

A **dream** is a memory consolidation pass: the agent reads past session context,
identifies key facts, decisions, and patterns, synthesizes a structured summary,
and (with human approval) writes it back as a persistent memory entry.

Dreams are **not** live agent launchers. They do not spawn agents or perform real
OS actions. They are a read-heavy, write-gated memory maintenance operation.

### Disambiguation: dreams vs PAO app

| Concept | What it is | Status |
|---------|-----------|--------|
| **Dream / memory consolidation** | Agent reads past context, synthesises, writes compact memory — with human approval | This lane; source_needed |
| **PAO app (N+6.24A)** | User-facing memory palace learning app (3D, LiDAR, PAO mnemonic system) | Separate future product; Tier-1-last; NOT this lane |

The PAO app is a distinct product that must never be conflated with agent dream/memory
consolidation infrastructure.

### Candidates

#### OpenDream / opendreams

**Status:** source_needed — operator must confirm exact repo. No URL is guessed.
Multiple unrelated projects use the name "opendreams".

**Intended pattern:**
- Walk committed context files / recent session logs.
- Identify recurring decisions, key facts, tool intakes.
- Generate a compact structured memory summary.
- Write to `14_context/00_main_memory/` only after human preview and approval.

**Evaluation criteria (once source confirmed):**
1. Local/file-based or does it require an external service?
2. Does it write to AGENTS.md or CLAUDE.md? If so, block that path.
3. Does it store sensitive data (credentials, health info, private paths)?
4. License compatibility with Ghoti MIT/source-visible approach?

#### dream-skill

**Status:** source_needed — operator must confirm exact skill source.

**Intended pattern (SKILL.md style):**
1. Read-only walk: scan `14_context/` for recent milestones and decisions.
2. Synthesis pass: produce a compact consolidated memory entry.
3. Approval gate: show the synthesised entry to the operator before any write.
4. Write (if approved): append to `14_context/00_main_memory/` only.

**Ghoti-native fallback:** if no external source is found, this pattern can be
implemented natively as a Claude Code SKILL.md with zero third-party dependencies.

**Hard constraint:** the skill must never auto-write `AGENTS.md` or `CLAUDE.md`.

#### dream-memory

**Status:** source_needed — operator must confirm exact repo.

**Intended pattern:**
- Persistent reflective memory layer with a consolidation loop.
- Reads prior context, identifies key facts and decisions.
- Writes a structured consolidated memory file.

**Constraints:**
- Must remain local and file-based.
- No cloud storage, no API keys.
- All writes previewed and human-approved.
- No sensitive data, secrets, private paths, or health details stored.

#### memory-lancedb-dreaming

**Status:** source_needed — operator must confirm exact repo. LanceDB adds a
vector-database dependency; evaluate whether local/file-only mode is available.

**Intended pattern:**
- Dream phases: consolidate short-term context into long-term structured memory.
- Memory promotion: move important context from active session into persistent index.

**Constraints:**
- Do not connect to cloud LanceDB.
- Evaluate local LanceDB mode before any intake.
- source_needed; no clone until source confirmed and local-mode verified.

### MemPalace impostor warning

Multiple unrelated projects and websites use the "MemPalace" or "memory-palace"
name. The operator should confirm the exact repo URL and cross-reference with the
official source before any intake. Do not clone a MemPalace repo from an unverified
site.

### Safety invariants for all dream / consolidation tools

- **Memory consolidation only** — not live agent launchers.
- **Read-only first** — no memory writes without human preview and approval.
- **Never auto-write** `AGENTS.md`, `CLAUDE.md`, or any other project-scope file.
- **No sensitive data** stored: no credentials, no health details, no private paths,
  no account data, no secrets of any kind.
- **Local and file-based** until a secret-management milestone.
- **PAO app (N+6.24A) is separate** — a future user-facing product, not agent memory.

---

## Memory Stack Integration Plan

### Current (no new tools installed)

```
Operator notes / decisions
         ↓
14_context/00_main_memory/   ← canonical source of truth
         ↓
ghoti_context_pack_builder   → compact_memory/generated/ (LLM-ready packs)
         ↓
ghoti_repo_knowledge_map     → repo_knowledge/generated/ (task bundles)
         ↓
Claude / Codex / Hermes      ← consume context packs
```

### Target (post-intake, tool-conditional)

```
Operator notes / decisions
         ↓
14_context/00_main_memory/
         ↓
[Claude Mem adapter]         ← if source confirmed + safe + local
[Obsidian bridge]            ← if source confirmed + read-only-first
[MemPalace index]            ← if repo found + file-based
         ↓
ghoti_context_pack_builder   → compact_memory/generated/
ghoti_repo_knowledge_map     → repo_knowledge/generated/
         ↓
Hermes status packet         ← grounds Hermes in mission state
         ↓
Claude / Codex / Hermes      ← consume context packs + status packets
```

### Hard rules (all layers)
- Every memory layer is **local and file-based** until a secret-management milestone.
- No external API, no cloud service, no API key in any memory layer.
- Hermes status packet (N+6.25B) remains the canonical grounding signal for Hermes.
- Memory Vault (`14_context/00_main_memory/`) is the source of truth; no tool overwrites
  it without an explicit approval gate.
- PAO app (N+6.24A) is a separate product lane and is never conflated with agent memory.
