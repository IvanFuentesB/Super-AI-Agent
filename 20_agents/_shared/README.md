# Shared Agent Assets

This area is for materials shared across multiple future agents:

- reusable prompt fragments
- naming conventions
- common policies
- shared schemas

Keep this small. If it grows too large, it is probably a sign that the material belongs in `14_context` instead.
