# Future Agents

Create future specialized automations and agent folders here by copying from:

- `../_templates/specialized_agent_template/`

Do not create agents directly in the root of `20_agents`.
